# Development Notes

This folder contains implementation notes from building `claude-session-replay`.

They are optional reading. For normal use, start with:

- the top-level `README.md`
- the CLI in `src/claude_session_replay/`
- the tests in `tests/`

These notes are kept as project history for future iteration.
